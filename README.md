
  # Design System and UI Guidelines

  This is a code bundle for Design System and UI Guidelines. The original project is available at https://www.figma.com/design/10pp7PnRhItyhxIS9jbcNE/Design-System-and-UI-Guidelines.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  